{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/baseball-game run build",
  "outputDirectory": "artifacts/baseball-game/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "REPLACE_WITH_FLY_URL/api/:path*" }
  ]
}
