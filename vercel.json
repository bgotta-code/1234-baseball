{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/baseball-game run build",
  "outputDirectory": "artifacts/baseball-game/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://1234-baseball.fly.dev/api/:path*" }
  ]
}
